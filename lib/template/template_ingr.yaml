apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: none
  annotations:
      nginx.ingress.kubernetes.io/proxy-body-size: "0"  
spec:
  rules:
  - host: none
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: none
              port:
                number: none
  

